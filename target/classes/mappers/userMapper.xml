<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.shop.mapper/UserMapper">

	<insert id="register" parameterType="UserVO">

		INSERT INTO users_tbl (
		user_id,
		user_name,
		user_pw,
		user_postcode,
		user_address,
		user_detailaddr,
		user_email,
		user_phonenum,
		user_mail_check,
		user_auth
		) VALUES (
		#{user_id},
		#{user_name},
		#{user_pw},
		#{user_postcode},
		#{user_address},
		#{user_detailaddr},
		#{user_email},
		#{user_phonenum},
		#{user_mail_check},
		#{user_auth}
		)
	</insert>

	<select id="login" parameterType="LoginDTO" resultType="UserVO">
		SELECT
		user_id,
		user_name,
		user_pw,
		user_postcode,
		user_address,
		user_detailaddr,
		user_phonenum,
		user_mail_check,
		user_auth,
		user_point,
		user_createdate,
		user_update,
		user_email
		FROM
		users_tbl
		where
		user_id = #{id}
	</select>
	
	<select id="check_id" parameterType="string" resultType="int">
		<![CDATA[
			select count(user_id) from users_tbl where user_id = #{user_id}
		]]>
	</select>
	
	<update id="modify" parameterType="UserVO">
		update users_tbl set
		user_name = #{user_name},
		user_email = #{user_email},
		user_postcode = #{user_postcode},
		user_address = #{user_address},
		user_detailaddr = #{user_detailaddr},
		user_phonenum = #{user_phonenum},
		user_mail_check = #{user_mail_check},
		user_update = SYSDATE
		where
		user_id = #{user_id}
	</update>
	
	<update id="pw_modify" parameterType="map">
		update users_tbl set
		user_pw = #{change_pw},
		user_update = SYSDATE
		where
		user_id = #{user_id}
	</update>
	
	<delete id="delete" parameterType="string">
		delete from users_tbl where user_id = #{id}
	</delete>
	
	
	<select id="find_id" parameterType="UserVO" resultType="string">
		select user_id from users_tbl
		where
		user_name = #{user_name} and
		user_email = #{user_email}
	</select>

</mapper>